import React from 'react';
import { createRoot } from 'react-dom/client';
import GenesisStyle2 from './GenesisStyle2';

createRoot(document.getElementById('root')!).render(
  <React.StrictMode><GenesisStyle2 /></React.StrictMode>,
);
